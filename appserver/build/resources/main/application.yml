server:
  port: 8080
file:
  upload:
    path:
spring:
  datasource:
    url: jdbc:mysql://appdb.cv2t5zdhnvok.ap-northeast-2.rds.amazonaws.com:3306/appdb?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: whdgns1002
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create # update create none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

  level:
    org:
      hibernate:
        type: trace
